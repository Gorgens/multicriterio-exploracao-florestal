source > if(a_ano17 = 'gap','gap','forest')

tempTarget > if(b_ano20 = 'gap','gap','forest')

target >

CASE 
  WHEN "source" IS 'gap' AND "tempTarget" IS 'gap' THEN 'gap t2'
  WHEN "source" IS 'forest' AND "tempTarget" IS 'gap' THEN 'gap t2'
  ELSE 'Recovered'
END
